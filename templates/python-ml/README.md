# {opportunity_title}

Scaffolded by **Astra** for the {organization} hackathon. This is a Python/ML
starter — read [BRIEF.md](BRIEF.md) for what you're building.

## Quick start

```bash
python -m venv .venv
source .venv/bin/activate    # Windows: .venv\Scripts\activate
pip install -r requirements.txt
python train.py
```

## Layout

- `train.py` — minimum-viable training loop. Replace with your model.
- `data/` — drop datasets here. The loader in `train.py` expects `data/train.csv`.
- `requirements.txt` — pinned ML stack.
- `BRIEF.md` — Astra's per-opportunity brief and Day-1 task list.
